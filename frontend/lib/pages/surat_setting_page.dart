import 'package:flutter/material.dart';

class SuratSettingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Setting Surat")),
      body: Center(
        child: Text("Setting Template / Jenis Surat"),
      ),
    );
  }
}
